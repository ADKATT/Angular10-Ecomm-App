import { WebSlidesDirective } from './web-slides.directive';

describe('WebSlidesDirective', () => {
    it('should create an instance', () => {
        const directive = new WebSlidesDirective();
        expect(directive).toBeTruthy();
    });
});
